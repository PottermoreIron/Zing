# Configure MQ starter properties in the consuming service configuration.
# RabbitMQ options include exchange type, publisher confirms, publisher returns,
# message TTL, dead-letter exchange, and retry limits.
# Kafka options include default partitions, replication factor, and retention.
